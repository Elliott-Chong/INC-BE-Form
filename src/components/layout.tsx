import React, { PropsWithChildren, useState } from "react";

const PageLayout = (props: PropsWithChildren) => {
  return <main className="overflow-none flex h-screen justify-center"></main>;
};

export default PageLayout;
